# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-dvbs2rx
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

add_subdirectory(ldpc_decoder)

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND dvbs2rx_sources
    bbdeheader_bb_impl.cc
    bbdescrambler_bb_impl.cc
    bch_decoder_bb_impl.cc
    bch.cc
    fec_params.cc
    gf.cc
    ldpc_decoder_bb_impl.cc
    pi2_bpsk.cc
    pl_descrambler.cc
    pl_frame_sync.cc
    pl_freq_sync.cc
    pl_signaling.cc
    plsync_cc_impl.cc
    reed_muller.cc
    rotator_cc_impl.cc
    symbol_sync_cc_impl.cc
    util.cc
    xfecframe_demapper_cb_impl.cc
)

set(dvbs2rx_sources "${dvbs2rx_sources}" PARENT_SCOPE)
if(NOT dvbs2rx_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT dvbs2rx_sources)

add_library(gnuradio-dvbs2rx SHARED ${dvbs2rx_sources})
target_link_libraries(gnuradio-dvbs2rx
    PRIVATE ${LDPC_LIBS}
    PRIVATE cpu_features
    PUBLIC gnuradio::gnuradio-runtime
    PUBLIC gnuradio::gnuradio-filter
  )
target_include_directories(gnuradio-dvbs2rx
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-dvbs2rx PROPERTIES DEFINE_SYMBOL "gnuradio_dvbs2rx_EXPORTS")

option(NATIVE_OPTIMIZATIONS "Optimize build for the local machine" ON)
if (NATIVE_OPTIMIZATIONS)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -march=native -Wall")
else()
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall")
endif()

if(APPLE)
    set_target_properties(gnuradio-dvbs2rx PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-dvbs2rx)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_dvbs2rx_sources
  qa_bch.cc
  qa_cdeque.cc
  qa_crc.cc
  qa_delay_line.cc
  qa_gf.cc
  qa_gf_util.cc
  qa_pi2_bpsk.cc
  qa_pl_frame_sync.cc
  qa_pl_freq_sync.cc
  qa_pl_signaling.cc
  qa_qpsk.cc
  qa_reed_muller.cc
  qa_symbol_sync_cc.cc
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-dvbs2rx)

if(NOT test_dvbs2rx_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_dvbs2rx_sources)

foreach(qa_file ${test_dvbs2rx_sources})
    GR_ADD_CPP_TEST("dvbs2rx_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
